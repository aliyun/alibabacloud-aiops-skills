# Required RAM Permissions

This skill is cloud read-only. Grant only these actions for the target CSAS scope:

```text
csas:ListUserDevices
csas:GetUserDevice
csas:GetUserDeviceWorkloadTrend
```

No create, update, delete, lock, recall, or other write action is required.
