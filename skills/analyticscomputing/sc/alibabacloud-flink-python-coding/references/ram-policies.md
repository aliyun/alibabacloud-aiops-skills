# RAM Permissions

Local development, API inspection, dependency builds, and artifact preparation invoke no Alibaba Cloud API and require no RAM permissions.

For a separately requested cloud operation, use the job-submission workflow and the permissions documented for that operation.
